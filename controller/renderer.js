export const renderHome = (req, res) => {
    res.render("home", {});
}

export const renderLogin = (req, res) => {
    res.render("login", {});
}

export const renderRegister = (req, res) => {
    res.render("register", {});
}